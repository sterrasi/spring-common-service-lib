# for the AppConfigurationLoggerTest

spring-common-service-lib:
  configuration-logger:
    enabled: true
    redaction-keys:
      - password
      - .id
      - secret
      - key
      - token
      - credential
      - certificate
      - "-redacted-"
    exclusion-keys:
      - sun.
      - java.

some-field: someValue
some-redacted-field: secretValue